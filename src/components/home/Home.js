import styles from "../styles/Home.module.css"


const { home_container } = styles;

const Body = () => {
  return (
    <div className={home_container}>
      SITE HOME
    </div>
  );};

export default Body;
