import styles from "../styles/Home.module.css"

const { home_container } = styles;

const Body = () => {
  return (
    <div className={home_container}>
      Turner site home
    </div>
  )};

export default Body;
